import java.util.Scanner;

public class keyboardLoop {
  public static void main(String[] args) {
    
    // int max = 0, min = max;
    // Scanner board = new Scanner(System.in);
    // int input = 1, numInputs = 0;

    // System.out.println("Enter all the numbers you want to enter. When you are done, enter a negative number to stop the loop");

    // while (input >= 0 ) {
    //   if (input > max)
    //     max = input;
    //   else if (input < min)
    //     min = input;
    //   input = board.nextInt();
    // }
    // System.out.println("The highest number you entered was " + max);
    // System.out.println("The lowest number you entered was " + min);

    String hi = "jijij";
    String lol = hi;

    System.out.println(hi + lol);

    hi = "jkj";

    System.out.println(hi + lol);
  }
}